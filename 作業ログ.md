# Asagiri Inventory 作業ログ

## 【重要なセキュリティルール】（作業前に必ず確認）

- このプロジェクトは商社の本番運用中システムです
- 以下を絶対に作業ログ・README・コミットメッセージ等に書かないでください
  - GAS Web App URL
  - スプレッドシートID
  - メールアドレス
  - その他の認証情報
- これらが必要な場合は `<<PLACEHOLDER>>` 形式で記述
- `.gitignore` に `.credentials.local` が入っていることを確認してから作業開始
- 作業完了時に `git diff` で機密情報の混入チェック

---

## プロジェクト概要

あさぎり商社向け在庫管理PWA。詳細は以下を参照：
- 全体設計図: `outputs/zaiko_prototype_design_v0.7.md`（Cowork側outputsフォルダ）
- Phase 2 実装メモ: `outputs/phase2_実装方針メモ.md`（同上）
- 引き継ぎメモ: `outputs/_引き継ぎメモ_次セッション用.md`（同上）

outputs フォルダのパス:
`C:\Users\south\AppData\Roaming\Claude\local-agent-mode-sessions\6c6b4ae6-ec2f-4cc3-9120-9df0e78d26e6\2fd78cb2-aeb1-48b0-b8e6-31ba588f876c\local_252175ba-b423-4ae7-b4b9-637eecd21e55\outputs\`

## デプロイ情報

| 項目 | 値 |
|---|---|
| GitHub リポジトリ | https://github.com/haru5512/asagiri-inventory |
| GitHub Pages URL | https://haru5512.github.io/asagiri-inventory/ |
| Pages ソースブランチ | main |

※ GAS URL・スプレッドシートID等の機密情報は `.credentials.local` に移動済み

## 現在の状態

### Phase 2: バーコードスキャン PWA — 実質完了

#### 完了した作業
1. PWA 初期実装（index.html, app.js, style.css, config.js, sw.js, manifest.json, icons/）
2. 設定画面に Web App URL 入力欄追加（localStorage 保存、config.js 編集不要）
3. ZXing-js 遅延読み込み（スキャン開始時のみロード、設定画面への影響を排除）
4. GAS API 接続問題の解決
   - デプロイ設定: Execute as=自分, アクセス=全員 に変更
   - GAS コード: findItemByBarcode の requireAuth() を削除（旧434行目）
5. スマホ実機確認完了
   - 手入力検索: IT001 → 品目情報表示 OK
   - バーコードスキャン: 市販品JAN読み取り → 未登録画面表示 OK

#### スキップした確認項目（動作に支障なし）
- QRコードスキャン（IT001埋め込みQR）
- PWA ホーム画面追加
- テストチェックリスト（Phase 2 メモ §7）の残り

### 設計上のポイント（次セッション向け）
- ZXing-js は v0.21.0 固定（CDN、遅延読み込み）
- `<video playsinline>` で iOS Safari 対応済み
- stopScan() を画面遷移時に呼んでカメラ停止
- GAS API は GET で `?action=findItemByBarcode&barcode=xxx`
- GAS の認証は Phase 2 では外している（findItemByBarcode のみ）。Phase 3 で要検討

## GAS 変更履歴

| 日時 | 変更内容 | 詳細 |
|---|---|---|
| 2026-05-15 | findItemByBarcode から requireAuth() を削除 | 外部fetchで認証不可のため |
| 2026-05-15 | 作業ログ.mdから機密情報を削除 | GAS URL・スプレッドシートID・メール等を`.credentials.local`に移動。git履歴も書き換え済み |

### Phase 3a: 入出庫記録 — 完了

#### 完了した作業
1. 設定画面に Gmail 入力欄追加（localStorage 保存、簡易認証）
2. 場所データのキャッシュ機能（getLocations API → localStorage、手動再取得ボタン）
3. トップメニュー改修（品目検索セクション + 入出庫セクション: 入庫/出庫/廃棄/移動）
4. 入出庫用スキャン/手入力画面（区分名をヘッダーに表示）
5. 数量入力画面（区分に応じた場所ドロップダウン: 入庫=任意, 出庫/廃棄=必須, 移動=2箇所）
6. 確認画面（全入力内容の一覧表示）
7. API 呼び出し（recordStockIn / recordStockOut / recordStockMove）
8. 完了画面（「続けてスキャン」で同じ区分・場所を保持）
9. ロット管理: 入庫時にロット番号・賞味期限入力
10. ロット管理: 廃棄時にロット選択ドロップダウン（APIから有効ロット取得）
11. ロット管理: 出庫時にFIFO引き当て結果表示

#### 認証方式
- Gmail パラメータ方式（設定画面で入力 → API リクエストに付与 → GAS 側で M_担当者照合）
- GAS 側の requireAuth() は recordStockIn / recordStockOut / recordStockMove / getLocations から削除が必要
- 本格認証は Stage 1 引き渡し時に Google 認証で対応

#### キャッシュ方針
- 場所一覧: 手動再取得のみ（Phase 3a）
- 自動キャッシュ更新（火曜起動時チェック等）は Phase 3b 以降

#### 設計ドキュメント
- 設計書: `docs/superpowers/specs/2026-05-17-phase3a-stock-io-design.md`
- 実装計画: `docs/superpowers/plans/2026-05-17-phase3a-stock-io.md`

### PC在庫管理画面 — 完了

#### 完了した作業
1. inventory.html — PC管理者向け在庫確認・印刷ページ（既存PWAとは独立）
2. 3タブ構成: 現在在庫 / 賞味期限 / 入出庫履歴
3. クライアント側フィルタ: カテゴリ, 品目名, 場所, 賞味期限○日以内, 期間, 区分
4. 印刷機能: A4横向き、ページ分割対応、フィルタ適用後のデータをそのまま印刷
5. 既存PWAの config.js + localStorage の API URL 設定を共有

#### GAS 側の新規API（実装済み）
- getCurrentStockList — 品目ごとの現在数量・場所・閾値アラート
- getAllLots — 全有効ロット一覧（残数量>0、賞味期限順）
- getTransactionHistory — 入出庫履歴（確定/承認済のみ、最新順）

#### 設計ドキュメント
- 設計書: `docs/superpowers/specs/2026-05-17-pc-inventory-view-design.md`
- 実装計画: `docs/superpowers/plans/2026-05-17-pc-inventory-view.md`

### 商品登録フォーム（PC管理画面） — 完了

#### 完了した作業
1. 現在在庫タブに「+ 新規登録」ボタン追加
2. モーダルで商品登録フォーム（品名/カテゴリ/単位/税率 等、全14項目）
3. バリデーション（品名・大カテゴリ・単位は必須）
4. 連続入力対応（「登録して続けて入力」でカテゴリ・単位・税率は保持）
5. GAS側: registerItem API（品目ID自動採番、M_品目に行追加）

#### 認証方式
- registerItem は gmail で M_担当者 照合のみ（パスワードチェックなし）
- パスワード認証はPC管理画面のログイン時のみ

#### ロット番号の自動採番（設計決定）
- 入庫時のロット番号はGAS側で自動生成（`L-YYYYMMDD-NNN`形式）
- 現場は賞味期限だけ入力すればよい（GAS側未実装、Phase 3b以降）

#### GAS実装時の注意点
- `SpreadsheetApp.getActiveSpreadsheet()` はWeb Appでは動かない → `getCurrentSpreadsheet()` を使う
- M_品目シートは空行にチェックボックス・ドロップダウンがある → `appendRow` は使わず `insertRowAfter` + `setValues` でデータ最終行の直下に挿入
- 税率列はスプシ側でパーセント書式 → フロントから 0.08/0.10 で送信（8/10 だと1000%になる）
- registerItem の認証は `findUserByEmail(params.gmail)` のみ（`requireGmailAuth` はパスワード必須なので使わない）

#### 設計ドキュメント
- 設計書: `docs/superpowers/specs/2026-05-17-item-registration-design.md`

### Phase 3b: 棚卸モード + キャッシュ + 設定拡充 — 完了

#### 完了した作業
1. PWA棚卸モード（5画面追加: 開始→スキャン→実数入力→サマリー→完了）
2. セッション管理（GAS側で自動ID生成、開始/終了/承認ステータス管理）
3. リアルタイム送信（1品スキャンごとにGAS API送信、エラー時は再送）
4. 同一品目の再スキャン対応（同セッション・同品目は上書き）
5. 未カウント品目の自動記録（セッション終了時、帳簿在庫>0の未スキャン品目を記録）
6. PC管理画面に棚卸タブ追加（セッション一覧、詳細モーダル、承認ボタン）
7. 棚卸承認で在庫調整（差異ありレコードごとにT_入出庫に「棚卸調整」追加）
8. キャッシュ自動更新（起動時に24時間経過チェック、バックグラウンド取得）
9. 設定画面拡充（場所データ最終取得日時の表示）

#### GAS側の作業 — 完了（2026-05-28）
- スプレッドシートに T_棚卸セッション / T_棚卸明細 シートを作成済み
- `GAScode_Phase3bR.txt` を丸ごとGASエディタに貼り付け（棚卸API 7本 + dispatchAction case含む）
- 新しいデプロイとして公開（旧デプロイはアーカイブ → URLが変更）
- PWA設定画面のWeb App URLを新URLに更新済み
- `?action=getStocktakeSessions` で正常応答を確認済み

#### トラブルシュート記録
- 「在庫管理API」はスタンドアロン型GASプロジェクト（スプシに紐づかない独立型）
- スプシの「拡張機能→Apps Script」からは開けない（無題のプロジェクトが開く）
- https://script.google.com から直接「在庫管理API」を開いて編集する
- スプシIDはスクリプトプロパティ `CURRENT_SPREADSHEET_ID` で管理（`setupSpreadsheetId()` で設定）
- 旧デプロイをアーカイブした後、新しいデプロイを作らないとURLが無効になる

#### GAS側の認証修正（Phase 3a で対応済み）
- recordStockIn, recordStockOut, recordStockMove: `requireGmailAuth` で対応済み
- getLocations: `requireAuth()` コメントアウト済み

#### 設計ドキュメント
- 設計書: `docs/superpowers/specs/2026-05-27-phase3b-design.md`
- 実装計画: `docs/superpowers/plans/2026-05-27-phase3b.md`

## ファイル構成

```
asagiri-inventory/
├── index.html       # メイン画面（SPA、16画面: 棚卸5画面追加）
├── app.js           # アプリロジック（入出庫+棚卸フロー、約1260行）
├── style.css        # スタイル（モバイルファースト + 棚卸用）
├── inventory.html   # PC在庫管理画面（4タブ: 現在在庫/賞味期限/入出庫履歴/棚卸）
├── inventory.js     # PC在庫管理ロジック（棚卸承認機能含む、約650行）
├── inventory.css    # PC在庫管理スタイル（棚卸用 + 印刷用CSS含む）
├── sw.js            # Service Worker（ネットワーク優先）
├── manifest.json    # PWAマニフェスト
├── config.js        # 設定（WEB_APP_URL、APP_VERSION 0.3.0）
├── icons/
│   ├── icon-192.png          # 仮アイコン（緑単色）
│   ├── icon-512.png          # 仮アイコン
│   └── icon-maskable-512.png # 仮アイコン
├── docs/
│   ├── gas-phase3b.js        # GAS追加コード（棚卸API 7本）
│   ├── gas-phase3b-手順書.md # GAS適用手順書
│   └── superpowers/
│       ├── specs/   # 設計書
│       └── plans/   # 実装計画
├── README.md
└── 作業ログ.md      # ← このファイル
```
